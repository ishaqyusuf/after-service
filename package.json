{
  "name": "after-service",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo build",
    "clean": "git clean -xdf node_modules .turbo apps/*/.next apps/*/.turbo packages/*/.turbo",
    "dev": "turbo dev --parallel",
    "dev:api": "turbo dev --filter=@afterservice/api",
    "dev:dashboard": "turbo dev --filter=@afterservice/dashboard",
    "dev:website": "turbo dev --filter=@afterservice/website",
    "format": "biome format --write .",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "turbo": "^2.5.0",
    "typescript": "^5.9.0"
  }
}
