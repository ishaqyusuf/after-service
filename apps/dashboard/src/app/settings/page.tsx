import { PageShell } from "@/lib/page-shell";

export default function SettingsPage() {
  return (
    <PageShell
      description="Workspace, team, notification, and security settings will be grouped here."
      title="Settings"
    />
  );
}
